spring:
  application:
    name: gateservice
  profiles:
    active:
      default
  cloud:
    config:
      enabled: true

zuul:
  ignored-headers: Access-Control-Allow-Credentials, Access-Control-Allow-Origin